{
  "kind": "collectionType",
  "collectionName": "friends",
  "info": {
    "singularName": "friend",
    "pluralName": "friends",
    "displayName": "friend",
    "description": ""
  },
  "options": {
    "draftAndPublish": false
  },
  "pluginOptions": {},
  "attributes": {
    "status": {
      "type": "enumeration",
      "enum": [
        "PENDING",
        "ACCEPTED",
        "DECLINED"
      ],
      "default": "PENDING"
    },
    "profile": {
      "type": "relation",
      "relation": "oneToOne",
      "target": "api::profile.profile"
    },
    "profilefk": {
      "type": "relation",
      "relation": "oneToOne",
      "target": "api::profile.profile"
    }
  }
}
